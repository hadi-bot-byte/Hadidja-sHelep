apiVersion: apps/v1
kind: Deployment
metadata:
  name: sos-service
  labels:
    app: sos-service
spec:
  replicas: {{ .Values.replicas | default 1 }}
  selector:
    matchLabels:
      app: sos-service
  template:
    metadata:
      labels:
        app: sos-service
    spec:
      containers:
        - name: sos-service
          image: {{ .Values.image }}
          imagePullPolicy: Never
          ports:
            - containerPort: {{ .Values.port }}
          env:
            - name: SERVICE_PORT
              value: "{{ .Values.port }}"
            - name: KAFKA_BOOTSTRAP
              value: {{ .Values.global.kafka.bootstrap }}
            - name: JWT_SECRET
              value: {{ .Values.global.jwtSecret }}
            - name: DB_PATH
              value: /data/sos.db
          volumeMounts:
            - name: data
              mountPath: /data
          livenessProbe:
            httpGet:
              path: /healthz
              port: {{ .Values.port }}
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /readyz
              port: {{ .Values.port }}
            initialDelaySeconds: 5
            periodSeconds: 5
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: sos-service-pvc
