apiVersion: v1
kind: Service
metadata:
  name: sos-service
spec:
  selector:
    app: sos-service
  ports:
    - protocol: TCP
      port: {{ .Values.port }}
      targetPort: {{ .Values.port }}
  type: ClusterIP
